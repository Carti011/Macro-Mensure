

CREATE TABLE MEDICOS (
    id NUMBER(19,0) GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    nome_completo VARCHAR2(150 CHAR) NOT NULL,
    crm VARCHAR2(20 CHAR) NOT NULL,
    CONSTRAINT pk_medicos PRIMARY KEY (id)
);

ALTER TABLE MEDICOS ADD CONSTRAINT uc_medicos_crm UNIQUE (crm);

ALTER TABLE MEDICOES_AMOSTRAS ADD medico_id NUMBER(19,0);

ALTER TABLE MEDICOES_AMOSTRAS ADD CONSTRAINT fk_medicoes_amostras_on_medico FOREIGN KEY (medico_id) REFERENCES MEDICOS (id);